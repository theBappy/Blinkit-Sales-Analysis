🛒 Blinkit Sales Analytics – SQL Project

⚠️ Replace with actual banner.
Want a custom one? Say “buddy make blinkit banner” and I’ll design it.

<br>
📘 Project Overview

This project performs a complete retail sales and outlet performance analysis on Blinkit (Grofers)-style data using SQL Server.

It uncovers insights around:

🧁 Product performance

🏪 Outlet performance

⭐ Ratings & visibility

📊 Item type vs revenue

🧮 Customer buying trends

🧹 Data cleaning & transformation

This project reflects real-world retail analytics, widely used in FMCG, supermarket intelligence, and supply chain analytics teams.

<br>
🗂 Dataset Overview (HTML Table)
Main Table: blinkit_data
<table> <tr><th>Column</th><th>Description</th></tr> <tr><td>Item_Identifier</td><td>Unique ID for each product</td></tr> <tr><td>Item_Weight</td><td>Weight of the product</td></tr> <tr><td>Item_Fat_Content</td><td>Low Fat / Regular (cleaned)</td></tr> <tr><td>Item_Visibility</td><td>Visibility score inside the store</td></tr> <tr><td>Item_Type</td><td>Product category (Dairy, Snacks, etc.)</td></tr> <tr><td>Item_MRP</td><td>Maximum Retail Price</td></tr> <tr><td>Outlet_Identifier</td><td>Unique store ID</td></tr> <tr><td>Outlet_Establishment_Year</td><td>Opening year of the outlet</td></tr> <tr><td>Outlet_Size</td><td>Small / Medium / High</td></tr> <tr><td>Outlet_Location_Type</td><td>Tier 1 / Tier 2 / Tier 3 cities</td></tr> <tr><td>Outlet_Type</td><td>Grocery / Supermarket Type 1/2/3</td></tr> <tr><td>Total_Sales</td><td>Total revenue generated by each item</td></tr> <tr><td>Rating</td><td>Customer rating for the item</td></tr> </table>
<br>
🧩 ER Diagram
                    ┌──────────────────────────┐
                    │       blinkit_data       │
                    │--------------------------│
                    │ Item_Identifier          │
                    │ Item_Fat_Content         │
                    │ Item_Type                │
                    │ Item_Visibility          │
                    │ Item_MRP                 │
                    │ Total_Sales              │
                    │ Rating                   │
                    │                          │
                    │ Outlet_Identifier        │
                    │ Outlet_Est_Year          │
                    │ Outlet_Size              │
                    │ Outlet_Location_Type     │
                    │ Outlet_Type              │
                    └──────────────────────────┘


Want PNG version?
➡️ Say “buddy generate ER PNG”

<br>
📊 KPI Summary
📌 Total Sales:

1.20 Million INR

📌 Average Sales per Item:

140 INR

📌 Total Number of Items:

8,523 items

📌 Average Rating:

4.0 / 5

<br>
🧼 Data Cleaning
UPDATE blinkit_data
SET Item_Fat_Content = 
    CASE 
        WHEN Item_Fat_Content IN ('LF','Low fat') THEN 'Low Fat'
        WHEN Item_Fat_Content = 'reg' THEN 'Regular'
        ELSE Item_Fat_Content
    END;

<br>
📈 Granular Analytics
🥛 1. Sales by Fat Content
SELECT
	Item_Fat_Content,
	CAST(SUM(Total_Sales) AS DECIMAL(10, 2)) AS Total_Sales,
	CAST(AVG(Total_Sales) AS DECIMAL(10, 1)) AS Avg_Sales,
	COUNT(*) AS No_Of_Items,
	CAST(AVG(Rating) AS DECIMAL(10, 2)) AS Avg_Rating
FROM blinkit_data
GROUP BY Item_Fat_Content
ORDER BY Total_Sales DESC;

<br>
🍱 2. Sales by Item Type
SELECT
	Item_Type,
	CAST(SUM(Total_Sales) AS DECIMAL(10, 2)) AS Total_Sales,
	CAST(AVG(Total_Sales) AS DECIMAL(10, 1)) AS Avg_Sales,
	COUNT(*) AS No_Of_Items,
	CAST(AVG(Rating) AS DECIMAL(10, 2)) AS Avg_Rating
FROM blinkit_data
GROUP BY Item_Type
ORDER BY Total_Sales DESC;

<br>
🏪 3. Fat Content Distribution by Outlet (Pivot)
SELECT 
	Outlet_Location_Type,
	ISNULL([Low Fat], 0) AS Low_Fat,
	ISNULL([Regular], 0) AS Regular
FROM (
	SELECT
		Outlet_Location_Type,
		Item_Fat_Content,
		CAST(SUM(Total_Sales) AS DECIMAL (10, 2)) AS Total_Sales
	FROM blinkit_data
	GROUP BY Outlet_Location_Type, Item_Fat_Content
) AS SourceTable
PIVOT (
	SUM(Total_Sales)
	FOR Item_Fat_Content IN ([Low Fat], [Regular])
) AS PivotTable
ORDER BY Outlet_Location_Type;

<br>
🧱 4. Sales by Outlet Establishment Year
SELECT
    Outlet_Establishment_Year,
    CAST(SUM(Total_Sales) AS DECIMAL(10, 2)) AS Total_Sales
FROM blinkit_data
GROUP BY Outlet_Establishment_Year
ORDER BY Outlet_Establishment_Year ASC;

<br>
📍 5. Sales by Outlet Size
SELECT
 Outlet_Size,
 CAST(SUM(Total_Sales) AS DECIMAL(10,2)) AS Total_Sales,
 CAST((SUM(Total_Sales) * 100.0 / SUM(SUM(Total_Sales)) OVER()) AS DECIMAL(10,2)) AS Sales_Percentage
FROM blinkit_data
GROUP BY Outlet_Size
ORDER BY Total_Sales DESC;

<br>
🌆 6. Sales by Outlet Location
SELECT
	Outlet_Location_Type,
	CAST(SUM(Total_Sales) AS DECIMAL(10, 2)) AS Total_Sales
FROM blinkit_data
GROUP BY Outlet_Location_Type
ORDER BY Total_Sales DESC;

<br>
🏷 7. Full Metrics by Outlet Type
SELECT
	Outlet_Type,
	CAST(SUM(Total_Sales) AS DECIMAL(10, 2)) AS Total_Sales,
	CAST(AVG(Total_Sales) AS DECIMAL(10, 0)) AS Avg_Sales,
	COUNT(*) AS No_of_Items,
	CAST(AVG(Rating) AS DECIMAL(10, 2)) AS Avg_Rating,
	CAST(AVG(Item_Visibility) AS DECIMAL(10, 2)) AS Item_Visibility
FROM blinkit_data
GROUP BY Outlet_Type
ORDER BY Total_Sales DESC;

<br>
📁 Folder Structure (HTML Table)
<table> <tr><th>Folder / File</th><th>Description</th></tr> <tr><td><b>data/</b></td><td>Contains the raw Blinkit dataset</td></tr> <tr><td>data/blinkit_data.csv</td><td>Main dataset file</td></tr> <tr><td><b>sql/</b></td><td>All SQL scripts used for analysis</td></tr> <tr><td>sql/cleaning.sql</td><td>Data preparation & cleaning script</td></tr> <tr><td>sql/kpis.sql</td><td>KPI calculation queries</td></tr> <tr><td>sql/item_analysis.sql</td><td>Item-level insights</td></tr> <tr><td>sql/outlet_analysis.sql</td><td>Outlet performance logic</td></tr> <tr><td>sql/fat_content_pivot.sql</td><td>Pivot transformation queries</td></tr> <tr><td><b>visuals/</b></td><td>Dashboard images, ER diagram, banner</td></tr> <tr><td><b>README.md</b></td><td>Main project documentation</td></tr> </table>
<br>
✨ Author

SH Haque
SQL Developer • Data Analyst • BI Dashboard Engineer
